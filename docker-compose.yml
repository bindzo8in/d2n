services:
  web:
    image: ghcr.io/bindzo8in/d2n:latest
    container_name: d2n
    restart: unless-stopped

    env_file:
      - .env

    networks:
      - web_network

networks:
  web_network:
    external: true